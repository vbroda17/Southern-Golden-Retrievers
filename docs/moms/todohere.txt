Auto-rotate every 5s (pause on hover)

Fade transition between images

Swipe gestures on mobile

Captions under dots (“1 / 3”)

Refactor this into a reusable function for all moms

Extract config (image path + list)

Add accessibility labels